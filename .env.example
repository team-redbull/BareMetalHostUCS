# ============================================================================
# BareMetalHost Generator Operator - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control!

# ============================================================================
# Logging Configuration
# ============================================================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# Buffer Management
# ============================================================================
# Maximum number of available (non-provisioned) BareMetalHosts in the cluster
MAX_AVAILABLE_SERVERS=20

# How often (in seconds) to check if buffered servers can be released
BUFFER_CHECK_INTERVAL=30

# ============================================================================
# Resource Deletion Configuration
# ============================================================================
# Control whether to delete resources (BMH, Secret, NMStateConfig) when BareMetalHostGenerator is deleted
# Set to 'false' to preserve resources after BareMetalHostGenerator deletion (useful for debugging or manual cleanup)
# Options: true, false, 1, 0, yes, no
DELETE_RESOURCES_ON_DELETE=true

# ============================================================================
# HP OneView Configuration
# ============================================================================
# Comment out or leave empty if not using HP OneView
ONEVIEW_IP=
ONEVIEW_USERNAME=administrator
ONEVIEW_PASSWORD=

# HP iLO BMC Credentials (used for all HP servers)
# These credentials are used in the BMC secret for BareMetalHost resources
HP_BMC_USERNAME=Administrator
HP_BMC_PASSWORD=

# ============================================================================
# Cisco UCS Central Configuration
# ============================================================================
# Comment out or leave empty if not using Cisco UCS
UCS_CENTRAL_IP=
UCS_CENTRAL_USERNAME=admin
UCS_CENTRAL_PASSWORD=

# UCS Manager credentials (used to connect to individual UCS Managers)
UCS_MANAGER_USERNAME=admin
UCS_MANAGER_PASSWORD=

# Cisco CIMC BMC Credentials (used for all Cisco servers)
# These credentials are used in the BMC secret for BareMetalHost resources
CISCO_BMC_USERNAME=admin
CISCO_BMC_PASSWORD=

# ============================================================================
# Dell OpenManage Enterprise (OME) Configuration
# ============================================================================
# Comment out or leave empty if not using Dell OME
OME_IP=
OME_USERNAME=admin
OME_PASSWORD=

# Dell iDRAC BMC Credentials (used for all Dell servers)
# These credentials are used in the BMC secret for BareMetalHost resources
DELL_BMC_USERNAME=root
DELL_BMC_PASSWORD=calvin

# ============================================================================
# Cisco Intersight Configuration
# ============================================================================
# Comment out or leave empty if not using Cisco Intersight.
# NOTE: uses the official 'intersight' Python SDK (in requirements.txt).
INTERSIGHT_API_ENDPOINT=https://intersight.com
INTERSIGHT_API_KEY_ID=
# Either the PEM private key text, or a path to the .pem file:
INTERSIGHT_API_SECRET=

# Cisco CIMC BMC Credentials for Intersight-managed servers (used in BMC secret).
# Intersight BareMetalHosts use a redfish-virtualmedia BMC address.
INTERSIGHT_BMC_USERNAME=admin
INTERSIGHT_BMC_PASSWORD=

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
#
# 1. Management System Credentials vs BMC Credentials:
#    - Management System Credentials (ONEVIEW_*, UCS_*, OME_*):
#      Used by the OPERATOR to login and query server MAC/IP information
#
#    - BMC Credentials (HP_BMC_*, DELL_BMC_*, CISCO_BMC_*):
#      Used by the BAREMETALHOST to control the physical servers (power, boot, etc)
#      These are stored in Kubernetes secrets and used by Metal3/Ironic
#
# 2. You must configure at least ONE vendor's management system credentials
#    AND the corresponding BMC credentials for that vendor.
#
# 3. BMC credentials are vendor-specific and static across all servers of that vendor.
#    For example, all HP servers use the same HP_BMC_USERNAME/HP_BMC_PASSWORD.
#
# 4. Credentials are stored as PLAIN TEXT in environment variables.
#    The operator will base64-encode them when creating Kubernetes secrets.
#
# 5. If BMC credentials are not set for a configured vendor, the operator will
#    fail with a clear error message when trying to create a BareMetalHost.
#
